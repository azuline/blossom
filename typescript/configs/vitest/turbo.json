{
  "extends": ["//"],
  "tasks": {
    // this is a dummy job to ensures that a change to the configs invlidate
    // all downstream test caches
    "typecheck": {
      "inputs": ["index.ts", "global.ts"]
    },
    "test": {
      "inputs": ["index.ts", "global.ts"]
    }
  }
}
