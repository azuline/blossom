{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"],
      "inputs": ["src/**/*.{ts,tsx}", "tsconfig.json", "vite.config.ts"]
    },
    "typecheck": {
      "inputs": ["src/**/*.{ts,tsx}", "tsconfig.json"]
    },
    "lint": {
      "inputs": ["src/**/*.{ts,tsx}", ".eslintrc.cjs"],
      // Depends on successfully building .d.ts files.
      "dependsOn": ["typecheck"]
    },
    "test": {
      "inputs": ["src/**/*.{ts,tsx}"]
    },
    "lintcheck": {
      "inputs": ["src/**/*.{ts,tsx}", ".eslintrc.cjs"],
      // Depends on successfully building .d.ts files.
      "dependsOn": ["typecheck"]
    },
    "start": {
      "cache": false,
      "persistent": true
    },
    "typewatch": {
      "cache": false,
      "persistent": true
    }
  },
  "globalDependencies": [
    "configs/typescript/base.json",
    "configs/eslint/.eslintrc.cjs",
    "dprint.json",
    "scripts/*"
  ]
}
